import { RouteLoading } from "@/components/layout/route-loading";

export default function Loading() {
  return <RouteLoading title="Teacher roll call" subtitle="Loading teacher page…" rows={2} />;
}
