import { RouteLoading } from "@/components/layout/route-loading";

export default function Loading() {
  return <RouteLoading title="Emergency check-in" subtitle="Loading student page…" rows={2} />;
}
